GUPTO PHASE 03K — STORY STUDIO SCROLL RESTORE

Base required: Phase 03J

Fixes
- Restores reliable desktop scrolling inside the Story Studio controls column.
- Makes the modal use an explicit viewport-safe grid height.
- Keeps the preview and header fixed while settings scroll independently.
- Adds safe bottom padding so every “Make it yours” item is reachable.
- Adds a subtle scrollbar, scroll containment and smooth touch scrolling.
- Preserves mobile full-sheet scrolling.

Install
1. Extract this ZIP into the project root and replace matching files.
2. Run: npm run build
3. Run: npm run dev

Database migration: Not required.

