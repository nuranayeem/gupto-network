GUPTO Phase 04J — Pixel-integrated owner Story plus

- Install this update over Phase 04I.
- The plus now sits pixel-perfectly within the avatar frame's lower-right edge.
- A controlled micro-gap separates the plus from the thick frame border,
  leaving small breathing space above and below the mark.
- Avatar size, image, radius, border and shadow remain identical to the other
  Story profile avatars.
- No database migration is required.

After replacing the files, run:
  npm run build
  npm run dev

Then hard-refresh the browser (Ctrl+Shift+R).
