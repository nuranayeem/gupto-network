GUPTO PHASE 03P — PNG-STYLE NONE SWATCH

Base required: Phase 03O

Polish
- Changes Make it yours → None into the same circular size as every colour swatch.
- Uses a grey-and-white PNG transparency checkerboard inside the circle.
- Removes the visible “None” text while preserving its accessible label.
- Keeps the existing purple selected ring and adds subtle hover feedback.

Install
1. Extract this ZIP into the project root and replace matching files.
2. Run: npm run build
3. Run: npm run dev

Database migration: Not required.

