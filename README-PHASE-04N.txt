GUPTO PHASE 04N — CLEAN PROFILE AVATAR

Install this update over Phase 04M.

Changes:
- Removes the purple active-story ring from the large Profile page avatar.
- Removes both the Story badge and plus badge from the large Profile page avatar.
- Keeps the avatar click action for viewing or adding a Story.
- Leaves the Home Story card and its integrated plus design unchanged.
- No database migration is required.

After replacing the included files, run:
  npm run build
  npm run dev

Then hard-refresh the browser with Ctrl+Shift+R.
