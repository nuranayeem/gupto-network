PHASE 01BK — Activity Filter Active Color Match Sidebar

Changed:
- Active All / Reactions / Comments / Replies tab background now uses var(--primary-soft), exactly the same color token used by the active Sidebar item.

Untouched:
- Filter icons, text, counts, hover/focus behavior, spacing, Manage Activity button, Activity logic/API/database.

Replace:
- app/globals.css
